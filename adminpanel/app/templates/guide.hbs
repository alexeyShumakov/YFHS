{{outlet}}
{{#link-to 'edit_guide' model.id class='btn btn-primary pull-right'}}edit{{/link-to}}
<h3>{{model.name}}</h3>
<hr>
<dl>
  <dt>name</dt>
  <dd>{{model.name}}</dd>

  <dt>body</dt>
  <dd>
    {{markdown-onebox text=model.body}}
  </dd>

  <dt>category</dt>
  <dd>{{#link-to 'guide_category' model.guideCategory.id}}{{model.guideCategory.name}}{{/link-to}}</dd>
</dl>
