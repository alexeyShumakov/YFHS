import { online } from '../../../helpers/online';
import { module, test } from 'qunit';

module('Unit | Helper | online');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = online(42);
  assert.ok(result);
});
