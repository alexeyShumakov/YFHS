import { formatedDate } from '../../../helpers/formated-date';
import { module, test } from 'qunit';

module('Unit | Helper | formated date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatedDate(42);
  assert.ok(result);
});
