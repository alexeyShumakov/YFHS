<div class="panel panel-default">
  <div class="panel-heading">
    {{news.title}}
    <i class=""> {{news.user.email}}</i>
    <button {{action 'delete'}}type="button" class="close" data-dismiss="modal" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
  <div class="panel-body">{{markdown news.body}}</div>
  <div class="panel-footer">
    {{#link-to 'single_news' news}}go to news {{/link-to}}
    {{like-comp model=news}}
  </div>
</div>

