<div class="panel panel-default">
  <div class="panel-heading">
    {{news.name}}
    <i class=""> {{news.user.email}}</i>
    <button {{action 'delete'}}type="button" class="close" data-dismiss="modal" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
  <div class="panel-body">{{markdown-onebox text=news.body}}</div>
  <div class="pull-right">{{vote-comp model=news}}</div>
  <div class="panel-footer">
    {{#link-to 'news' news.id}}go to news {{/link-to}}
  </div>
</div>

