<div class="col-md-8">
  <div class="list-group">
    {{#each model.decks as |deck|}}
      <div class="deck-list-item list-group-item">
        {{deck-item deck=deck}}
      </div>
    {{/each}}
  </div>
  {{outlet}}
</div>

<div class="col-md-4">
  {{render 'sidebar'}}
</div>