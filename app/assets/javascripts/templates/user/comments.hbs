<ul class="list-unstyled">
  {{#each model as |comment|}}
    <li class="bordered margin-bottom">
      {{history-comment comment=comment action='transitionToEntity'}}
    </li>
  {{else}}
    <h3>Нет комментариев.</h3>
  {{/each}}
</ul>
{{infinity-loader queryParams=params model=model modelName='comment'}}

